var oso;
var hospital;
var tren;
var incendio;
var muelte;
var boliche;
var siesta;
var derecha;
var izquierda;

if (oso == "distraido") {
    console.log(tren);
    if (tren == izquierda) {
        console.log(incendio + muelte);
    } else if (tren == derecha) {
        console.log(bolich);
    } else {
        console.log(siesta);
    }
}

if (oso == "despierto") {
    console.log(hospital + boliche);
}